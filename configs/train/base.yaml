train_split: train
eval_split: val
checkpoint_dir: outputs/tiqs-qformer
connector_output_path: chartvqa/models/TiQS/chartqa-qformer-adapter.pt

train_batch_size: 2
eval_batch_size: 2
gradient_accumulation_steps: 4
num_epochs: 3
learning_rate: 0.0002
weight_decay: 0.0
warmup_ratio: 0.03
is_fp16: true
is_bf16: false

logging_steps: 20
save_steps: 200
eval_steps: 200
save_total_limit: 2
evaluation_strategy: steps
report_to: null

dataloader_num_workers: 4
gradient_checkpointing: false
load_best_model_at_end: false
metric_for_best_model: null

max_train_samples: null
max_eval_samples: 256
resume_from_checkpoint: null
max_train_seconds: null

report_path: train_report.json
wandb_log: false # set to true to enable Weights & Biases logging
wandb_artifact_name: tiqs-connector
wandb_artifact_type: connector
